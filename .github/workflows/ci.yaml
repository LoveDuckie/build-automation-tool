# .github/workflows/dotnet.yml
name: .NET CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.x'  # Specify the version or use 'latest' for the most recent

    - name: Install dependencies
      run: dotnet restore

    - name: Lint the code
      run: cd Tool/BuildAutomationTool && dotnet format --verify-no-changes
      continue-on-error: true

    - name: Build the project
      run: cd Tool/BuildAutomationTool && dotnet build --no-restore --configuration Release

    - name: Run unit tests
      run: cd Tool/BuildAutomationTool && dotnet test --no-build --configuration Release --verbosity normal

    - name: Package the project
      run: cd Tool/BuildAutomationTool && dotnet pack --no-build --configuration Release -o ./artifacts

    - name: Publish package to GitHub Packages
      if: github.ref == 'refs/heads/main' && success()  # Publish only on successful builds of the main branch
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: dotnet nuget push ./artifacts/*.nupkg --source "github"
